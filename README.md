# Magalhães Lopes - Advogados — Website

Auto-generated and deployed by [SiteStarter](http://localhost:3000).

## Environment Variables

Set these in your Vercel project settings:

| Variable | Description |
|---|---|
| `SUPABASE_URL` | Supabase project URL |
| `SUPABASE_SERVICE_KEY` | Supabase service role key |
| `NEXT_PUBLIC_SITE_ID` | Site ID (4) |

## Routes

- `/` — Main website
- `/blog` — Blog listing
- `/blog/[slug]` — Individual blog post
- `/api/reservations` — POST reservation submissions

## Content Updates

To update the site content, redeploy from the SiteStarter admin dashboard.
Blog posts and reservations update in real-time (no redeploy needed).